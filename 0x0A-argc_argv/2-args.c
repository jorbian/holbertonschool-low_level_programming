#include <stdio.h>
/**
 * main - ads f
 * @argc: number of arguments
 * @argv: asdf asdf
 *
 * Return: zero
 */
int main(int argc, char *argv[])
{
	int i;

	for (i = 0; i < argc; i++)
		printf("%s\n", argv[i]);

	return (0);
}
