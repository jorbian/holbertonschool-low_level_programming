#include <stdio.h>
/**
* print_last_digit - The ritually descriptor
* @n: an i(n)teger. any i(n)teger you wantses
*
* Return: returns the last digit of the integer whatever that happens to be.
*/

int print_last_digit(int n)
{
	return (n % 10);
}
