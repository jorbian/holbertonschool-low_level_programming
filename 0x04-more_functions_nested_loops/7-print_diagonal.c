#include <stdio.h>

/** 
* print_diagonoal - asdf 
* @size: a;skldfhja;lksdhjf
*
* Return: it don't return shit
*/
void print_diagonal(int size)
{
	int i, j;

	for (i = 0; i < i; i++)
	{
		for (j = 0; j < i; j++) 
		{
			_putchar(' ');
		}
		_putchar(92);
		if (i < (n - 1))
			_putchar('\x0a');
	}
	_putchar('\x0a');
}
